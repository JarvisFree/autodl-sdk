# autodl-sdk
AutoDL Python SDK for automation — manage GPU instances, upload code, and run remote commands without touching the web console. Supports base / community / private images, SSH file transfer, and full lifecycle automation.
